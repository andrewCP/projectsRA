\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}

\usepackage{fontspec}
\usepackage[spanish,es-noshorthands]{babel}

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{bm}
\usepackage{booktabs}
\usepackage{tikz}
\usepackage{tikz-3dplot}
\usetikzlibrary{arrows.meta,positioning,calc}
\usepackage{siunitx}

\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue]{hyperref}

\sisetup{output-decimal-marker={.}, group-separator={\,}}

% ---- Estilos para el diagrama de bloques -----------------------------
\tikzset{
  block/.style = {draw, thick, rectangle, minimum height=2.6em, minimum width=2.6em},
  sum/.style   = {draw, thick, circle, inner sep=0pt, minimum size=6mm},
  input/.style = {coordinate},
  output/.style= {coordinate},
  punto/.style = {circle, fill, inner sep=1.2pt}
}

\newcommand{\Lap}{\mathcal{L}}



\title{Formalismo Matemático RA}
\author{Estudiantes RA 2026 2 }
\date{}

\begin{document}

\maketitle


\section{Paso 5,6 y 7 ejercicio 4 del parcial}

\paragraph{Juan Camilo Tovar Chaves 1804242}


\textbf{Paso 5: Despejar el término de las derivadas.} \\
Pasamos el vector resultante del producto cruz al lado derecho (restando, lo que cambia sus signos) y pre-multiplicamos toda la ecuación por la matriz inversa de inercia $I^{-1}$:
\begin{equation*}
    \begin{pmatrix} \dot{p} \\ \dot{q} \\ \dot{r} \end{pmatrix} =
    I^{-1} \begin{pmatrix}
    I_{xz}pq - I_{zz}qr + I_{yy}qr \\
    -I_{xz}p^2 + I_{zz}rp - I_{xx}pr - I_{xz}r^2 \\
    -I_{yy}qp + I_{xx}pq - I_{xz}qr
    \end{pmatrix} + I^{-1} \begin{pmatrix} \tau_x \\ \tau_y \\ \tau_z \end{pmatrix}
\end{equation*}

\vspace{0.3cm}
\textbf{Paso 6: Encontrar la matriz inversa $I^{-1}$.} \\
Determinamos la inversa de la matriz de inercia $I$. Al tener varios ceros, se invierte fácilmente por bloques, resultando en:
\begin{equation*}
    I^{-1} = \begin{pmatrix}
    \frac{I_{zz}}{I_{xx}I_{zz} - I_{xz}^2} & 0 & \frac{I_{xz}}{I_{xx}I_{zz} - I_{xz}^2} \\[10pt]
    0 & \frac{1}{I_{yy}} & 0 \\[10pt]
    \frac{I_{xz}}{I_{xx}I_{zz} - I_{xz}^2} & 0 & \frac{I_{xx}}{I_{xx}I_{zz} - I_{xz}^2}
    \end{pmatrix}
\end{equation*}

\vspace{0.3cm}
\textbf{Paso 7: Reemplazar $I^{-1}$ y obtener las ecuaciones escalares para $\dot{p}, \dot{q}, \dot{r}$.} \\
Multiplicamos la matriz inversa $I^{-1}$ obtenida en el Paso 6 por los vectores de la ecuación del Paso 5. Para simplificar la notación, definimos el denominador común $\Gamma = I_{xx}I_{zz} - I_{xz}^2$. Efectuando el producto matriz-vector fila por fila, obtenemos las tres ecuaciones escalares:

\begin{align*}
    \dot{p} &= \frac{I_{zz}}{\Gamma} \big( I_{xz}pq - I_{zz}qr + I_{yy}qr \big) + \frac{I_{xz}}{\Gamma} \big( -I_{yy}qp + I_{xx}pq - I_{xz}qr \big) + \frac{I_{zz}}{\Gamma}\tau_x + \frac{I_{xz}}{\Gamma}\tau_z \\[10pt]
    \dot{q} &= \frac{1}{I_{yy}} \big( -I_{xz}p^2 + I_{zz}rp - I_{xx}pr + I_{xz}r^2 \big) + \frac{1}{I_{yy}}\tau_y \\[10pt]
    \dot{r} &= \frac{I_{xz}}{\Gamma} \big( I_{xz}pq - I_{zz}qr + I_{yy}qr \big) + \frac{I_{xx}}{\Gamma} \big( -I_{yy}qp + I_{xx}pq - I_{xz}qr \big) + \frac{I_{xz}}{\Gamma}\tau_x + \frac{I_{xx}}{\Gamma}\tau_z
\end{align*}





\section{Marcos de referencia y matrices de rotation}
\paragraph{Osuna}
\subsection*{Expresión del vector en ambos marcos}

\begin{align}
  \vec{a} &= a_x^{I}\,\hat{\imath}^{\,I} + a_y^{I}\,\hat{\jmath}^{\,I} + a_z^{I}\,\hat{k}^{\,I} \\[4pt]
  \vec{a} &= a_x^{b}\,\hat{\imath}^{\,b} + a_y^{b}\,\hat{\jmath}^{\,b} + a_z^{b}\,\hat{k}^{\,b} \\[4pt]
  a_x^{I}\,\hat{\imath}^{\,I} + a_y^{I}\,\hat{\jmath}^{\,I} + a_z^{I}\,\hat{k}^{\,I}
  &= a_x^{b}\,\hat{\imath}^{\,b} + a_y^{b}\,\hat{\jmath}^{\,b} + a_z^{b}\,\hat{k}^{\,b}
\end{align}

\subsection*{Producto punto con $\hat{\imath}^{\,b}$}

\begin{align}
  \hat{\imath}^{\,b}\cdot\left( a_x^{I}\,\hat{\imath}^{\,I} + a_y^{I}\,\hat{\jmath}^{\,I} + a_z^{I}\,\hat{k}^{\,I} \right)
  &= \hat{\imath}^{\,b}\cdot\left( a_x^{b}\,\hat{\imath}^{\,b} + a_y^{b}\,\hat{\jmath}^{\,b} + a_z^{b}\,\hat{k}^{\,b} \right) \\[6pt]
  a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{\imath}^{\,b}\right)
  + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{\imath}^{\,b}\right)
  + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{\imath}^{\,b}\right)
  &= a_x^{b}\left(\hat{\imath}^{\,b}\cdot\hat{\imath}^{\,b}\right)
  + a_y^{b}\left(\hat{\jmath}^{\,b}\cdot\hat{\imath}^{\,b}\right)
  + a_z^{b}\left(\hat{k}^{\,b}\cdot\hat{\imath}^{\,b}\right) \\[6pt]
  \hat{\imath}^{\,b}\cdot\hat{\imath}^{\,b} = 1, \qquad
  \hat{\jmath}^{\,b}\cdot\hat{\imath}^{\,b} &= 0, \qquad
  \hat{k}^{\,b}\cdot\hat{\imath}^{\,b} = 0 \\[6pt]
  a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{\imath}^{\,b}\right)
  + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{\imath}^{\,b}\right)
  + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{\imath}^{\,b}\right)
  &= a_x^{b}(1) + a_y^{b}(0) + a_z^{b}(0) \\[6pt]
  &= a_x^{b} + 0 + 0
\end{align}

\begin{equation}
  \boxed{\;a_x^{b} = a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{\imath}^{\,b}\right)
  + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{\imath}^{\,b}\right)
  + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{\imath}^{\,b}\right)\;}
\end{equation}

\subsection*{Producto punto con $\hat{\jmath}^{\,b}$}

\begin{align}
  \hat{\jmath}^{\,b}\cdot\left( a_x^{I}\,\hat{\imath}^{\,I} + a_y^{I}\,\hat{\jmath}^{\,I} + a_z^{I}\,\hat{k}^{\,I} \right)
  &= \hat{\jmath}^{\,b}\cdot\left( a_x^{b}\,\hat{\imath}^{\,b} + a_y^{b}\,\hat{\jmath}^{\,b} + a_z^{b}\,\hat{k}^{\,b} \right) \\[6pt]
  a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
  + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
  + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
  &= a_x^{b}\left(\hat{\imath}^{\,b}\cdot\hat{\jmath}^{\,b}\right)
  + a_y^{b}\left(\hat{\jmath}^{\,b}\cdot\hat{\jmath}^{\,b}\right)
  + a_z^{b}\left(\hat{k}^{\,b}\cdot\hat{\jmath}^{\,b}\right) \\[6pt]
  \hat{\imath}^{\,b}\cdot\hat{\jmath}^{\,b} = 0, \qquad
  \hat{\jmath}^{\,b}\cdot\hat{\jmath}^{\,b} &= 1, \qquad
  \hat{k}^{\,b}\cdot\hat{\jmath}^{\,b} = 0 \\[6pt]
  a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
  + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
  + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
  &= a_x^{b}(0) + a_y^{b}(1) + a_z^{b}(0) \\[6pt]
  &= 0 + a_y^{b} + 0
\end{align}

\begin{equation}
  \boxed{\;a_y^{b} = a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
  + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
  + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{\jmath}^{\,b}\right)\;}
\end{equation}

\subsection*{Producto punto con $\hat{k}^{\,b}$}

\begin{align}
  \hat{k}^{\,b}\cdot\left( a_x^{I}\,\hat{\imath}^{\,I} + a_y^{I}\,\hat{\jmath}^{\,I} + a_z^{I}\,\hat{k}^{\,I} \right)
  &= \hat{k}^{\,b}\cdot\left( a_x^{b}\,\hat{\imath}^{\,b} + a_y^{b}\,\hat{\jmath}^{\,b} + a_z^{b}\,\hat{k}^{\,b} \right) \\[6pt]
  a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{k}^{\,b}\right)
  + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{k}^{\,b}\right)
  + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{k}^{\,b}\right)
  &= a_x^{b}\left(\hat{\imath}^{\,b}\cdot\hat{k}^{\,b}\right)
  + a_y^{b}\left(\hat{\jmath}^{\,b}\cdot\hat{k}^{\,b}\right)
  + a_z^{b}\left(\hat{k}^{\,b}\cdot\hat{k}^{\,b}\right) \\[6pt]
  \hat{\imath}^{\,b}\cdot\hat{k}^{\,b} = 0, \qquad
  \hat{\jmath}^{\,b}\cdot\hat{k}^{\,b} &= 0, \qquad
  \hat{k}^{\,b}\cdot\hat{k}^{\,b} = 1 \\[6pt]
  a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{k}^{\,b}\right)
  + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{k}^{\,b}\right)
  + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{k}^{\,b}\right)
  &= a_x^{b}(0) + a_y^{b}(0) + a_z^{b}(1) \\[6pt]
  &= 0 + 0 + a_z^{b}
\end{align}

\begin{equation}
  \boxed{\;a_z^{b} = a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{k}^{\,b}\right)
  + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{k}^{\,b}\right)
  + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{k}^{\,b}\right)\;}
\end{equation}

\subsection*{Forma matricial}

\begin{align}
  a_x^{b} &= a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{\imath}^{\,b}\right)
           + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{\imath}^{\,b}\right)
           + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{\imath}^{\,b}\right) \\
  a_y^{b} &= a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
           + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{\jmath}^{\,b}\right)
           + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{\jmath}^{\,b}\right) \\
  a_z^{b} &= a_x^{I}\left(\hat{\imath}^{\,I}\cdot\hat{k}^{\,b}\right)
           + a_y^{I}\left(\hat{\jmath}^{\,I}\cdot\hat{k}^{\,b}\right)
           + a_z^{I}\left(\hat{k}^{\,I}\cdot\hat{k}^{\,b}\right)
\end{align}

\begin{equation}
  \begin{bmatrix} a_x^{b} \\[4pt] a_y^{b} \\[4pt] a_z^{b} \end{bmatrix}
  =
  \begin{bmatrix}
    \hat{\imath}^{\,I}\cdot\hat{\imath}^{\,b} & \hat{\jmath}^{\,I}\cdot\hat{\imath}^{\,b} & \hat{k}^{\,I}\cdot\hat{\imath}^{\,b} \\[5pt]
    \hat{\imath}^{\,I}\cdot\hat{\jmath}^{\,b} & \hat{\jmath}^{\,I}\cdot\hat{\jmath}^{\,b} & \hat{k}^{\,I}\cdot\hat{\jmath}^{\,b} \\[5pt]
    \hat{\imath}^{\,I}\cdot\hat{k}^{\,b}      & \hat{\jmath}^{\,I}\cdot\hat{k}^{\,b}      & \hat{k}^{\,I}\cdot\hat{k}^{\,b}
  \end{bmatrix}
  \begin{bmatrix} a_x^{I} \\[4pt] a_y^{I} \\[4pt] a_z^{I} \end{bmatrix}
\end{equation}

\begin{equation}
  \vec{a}^{\,b} = \bm{R}^{b}_{I}\,\vec{a}^{\,I}
\end{equation}

\subsection*{Evaluación para $\psi$ (guiñada)}

\begin{align}
  \hat{\imath}^{\,I}\cdot\hat{\imath}^{\,b} &= \cos(\psi) \\[4pt]
  \hat{\jmath}^{\,I}\cdot\hat{\imath}^{\,b} &= \cos(90^{\circ}-\psi)
     = \cos(90^{\circ})\cos(\psi) + \sin(90^{\circ})\sin(\psi)
     = (0)\cos(\psi) + (1)\sin(\psi) = \sin(\psi) \\[4pt]
  \hat{k}^{\,I}\cdot\hat{\imath}^{\,b} &= \cos(90^{\circ}) = 0 \\[8pt]
  \hat{\imath}^{\,I}\cdot\hat{\jmath}^{\,b} &= \cos(90^{\circ}+\psi)
     = \cos(90^{\circ})\cos(\psi) - \sin(90^{\circ})\sin(\psi)
     = (0)\cos(\psi) - (1)\sin(\psi) = -\sin(\psi) \\[4pt]
  \hat{\jmath}^{\,I}\cdot\hat{\jmath}^{\,b} &= \cos(\psi) \\[4pt]
  \hat{k}^{\,I}\cdot\hat{\jmath}^{\,b} &= \cos(90^{\circ}) = 0 \\[8pt]
  \hat{\imath}^{\,I}\cdot\hat{k}^{\,b} &= \cos(90^{\circ}) = 0 \\[4pt]
  \hat{\jmath}^{\,I}\cdot\hat{k}^{\,b} &= \cos(90^{\circ}) = 0 \\[4pt]
  \hat{k}^{\,I}\cdot\hat{k}^{\,b} &= \cos(0^{\circ}) = 1
\end{align}

\begin{equation}
  \begin{bmatrix} a_x^{b} \\[4pt] a_y^{b} \\[4pt] a_z^{b} \end{bmatrix}
  =
  \begin{bmatrix}
    \cos(\psi) & \sin(\psi) & 0 \\[4pt]
   -\sin(\psi) & \cos(\psi) & 0 \\[4pt]
    0          & 0          & 1
  \end{bmatrix}
  \begin{bmatrix} a_x^{I} \\[4pt] a_y^{I} \\[4pt] a_z^{I} \end{bmatrix}
\end{equation}

\begin{align}
  a_x^{b} &= a_x^{I}\cos(\psi) + a_y^{I}\sin(\psi) + a_z^{I}(0)
           = a_x^{I}\cos(\psi) + a_y^{I}\sin(\psi) \\[4pt]
  a_y^{b} &= -a_x^{I}\sin(\psi) + a_y^{I}\cos(\psi) + a_z^{I}(0)
           = -a_x^{I}\sin(\psi) + a_y^{I}\cos(\psi) \\[4pt]
  a_z^{b} &= a_x^{I}(0) + a_y^{I}(0) + a_z^{I}(1) = a_z^{I}
\end{align}

\begin{equation}
  \left(\bm{R}^{b}_{I}\right)^{-1} = \left(\bm{R}^{b}_{I}\right)^{\!\top} = \bm{R}^{I}_{b},
  \qquad
  \det\!\left(\bm{R}^{b}_{I}\right) = 1
\end{equation}



\section{Matrices de rotación y velocidades angulares}
\paragraph{Carol Tatiana Jerez González}
La orientación del vehículo se obtiene mediante tres rotaciones sucesivas correspondientes a los ángulos de Euler.

\subsection*{Rotación alrededor del eje $x$}
La matriz de rotación correspondiente es

\[
R^{b}_{v_2}=
\begin{bmatrix}
1 & 0 & 0\\
0 & \cos\phi & \sin\phi\\
0 & -\sin\phi & \cos\phi
\end{bmatrix}
\]

La velocidad angular asociada es
\[
{}^{b}\boldsymbol{\omega}_{v_2}
=
\dot{\phi}\,
\hat{x}^{\,b}
\]

\subsection*{Rotación alrededor del eje $y$}
La segunda rotación se representa mediante

\[
R^{v_2}_{v_1}=
\begin{bmatrix}
\cos\theta & 0 & -\sin\theta\\
0 & 1 & 0\\
\sin\theta & 0 & \cos\theta
\end{bmatrix}
\]
La velocidad angular asociada es

\[
{}^{v_2}\boldsymbol{\omega}_{v_1}
=
\dot{\theta}\,
\hat{y}^{\,v_2}
\]

\subsection*{Rotación alrededor del eje $z$}
La tercera rotación está dada por

\[
R^{v_1}_{I}=
\begin{bmatrix}
\cos\psi & -\sin\psi & 0\\
\sin\psi & \cos\psi & 0\\
0 & 0 & 1
\end{bmatrix}
\]

La velocidad angular correspondiente es
\[
{}^{v_1}\boldsymbol{\omega}_{I}
=
\dot{\psi}\,
\hat{z}^{\,v_1}
\]

La velocidad angular total es
\[
{}^{b}\boldsymbol{\omega}_{I}
=
{}^{b}\boldsymbol{\omega}_{v_2}
+
{}^{v_2}\boldsymbol{\omega}_{v_1}
+
{}^{v_1}\boldsymbol{\omega}_{I}
\]
La velocidad lineal en el marco inercial se obtiene mediante

\[
\begin{bmatrix}
\dot{P}_N\\
\dot{P}_E\\
\dot{P}_D
\end{bmatrix}
=
R^{I}_{b}
\begin{bmatrix}
u\\
v\\
w\\
\end{bmatrix}
\]
\paragraph{Andres Felipe Valderrama Medina}
\begin{equation}
\begin{bmatrix}
{a_x}^{b} \\
{a_y}^{b} \\
{a_z}^{b}
\end{bmatrix}
=
\begin{bmatrix}
\hat{\imath}^{I}\cdot\hat{\imath}^{b} &
\hat{\jmath}^{I}\cdot\hat{\imath}^{b} &
\hat{k}^{I}\cdot\hat{\imath}^{b}
\\
\hat{\imath}^{I}\cdot\hat{\jmath}^{b} &
\hat{\jmath}^{I}\cdot\hat{\jmath}^{b} &
\hat{k}^{I}\cdot\hat{\jmath}^{b}
\\
\hat{\imath}^{I}\cdot\hat{k}^{b} &
\hat{\jmath}^{I}\cdot\hat{k}^{b} &
\hat{k}^{I}\cdot\hat{k}^{b}
\end{bmatrix}
\begin{bmatrix}
{a_x}^{I} \\
{a_y}^{I} \\
{a_z}^{I}
\end{bmatrix}
\end{equation}
\begin{equation}
\begin{bmatrix}
a_x^{b} \\
a_y^{b} \\
a_z^{b}
\end{bmatrix}
=
\begin{bmatrix}
\cos(\psi) & \sin(\psi) & 0 \\
-\sin(\psi) & \cos(\psi) & 0 \\
0 & 0 & 1
\end{bmatrix}
\begin{bmatrix}
a_x^{I} \\
a_y^{I} \\
a_z^{I}
\end{bmatrix}
\end{equation}
\[
\vec{a}^{\,b}
=
R_{I}^{b}\vec{a}^{\,I}
\]

\[
\left(R_{I}^{b}\right)^{-1}
=
\left(R_{I}^{b}\right)^{T}
=
R_{b}^{I}
\]

\[
\det\left(R_{I}^{b}\right)=1
\]



\section*{Cinemática y Transformación de Coordenadas}
\paragraph{David Santiago Sanabria Romero}
% --- 1. Transformación v2 -> b ---
\subsection*{1. Transformación del marco $v_2$ al marco del cuerpo $b$}
\begin{equation}
\begin{bmatrix}
\hat{x}_{v2} \\
\hat{y}_{v2} \\
\hat{z}_{v2}
\end{bmatrix}
=
\begin{bmatrix}
1 & 0 & 0 \\
0 & \cos\phi & -\sin\phi \\
0 & \sin\phi & \cos\phi
\end{bmatrix}
\begin{bmatrix}
\hat{x}_b \\
\hat{y}_b \\
\hat{z}_b
\end{bmatrix}
\end{equation}

Proyección del eje $\hat{y}_{v2}$:
\begin{equation}
\hat{y}_{v2} = \cos(\phi)\hat{y}_b - \sin(\phi)\hat{z}_b
\end{equation}

\vspace{0.3cm}

% --- 2. Transformación v1 -> b ---
\subsection*{2. Transformación del marco $v_1$ al marco del cuerpo $b$}

Producto de matrices de rotación $R_y(\theta) \cdot R_x(\phi)$:
\begin{equation}
\begin{bmatrix}
\hat{x}_{v1} \\
\hat{y}_{v1} \\
\hat{z}_{v1}
\end{bmatrix}
=
\begin{bmatrix}
\cos\theta & 0 & \sin\theta \\
0 & 1 & 0 \\
-\sin\theta & 0 & \cos\theta
\end{bmatrix}
\begin{bmatrix}
1 & 0 & 0 \\
0 & \cos\phi & -\sin\phi \\
0 & \sin\phi & \cos\phi
\end{bmatrix}
\begin{bmatrix}
\hat{x}_b \\
\hat{y}_b \\
\hat{z}_b
\end{bmatrix}
\end{equation}

Matriz de transformación resultante:
\begin{equation}
\begin{bmatrix}
\hat{x}_{v1} \\
\hat{y}_{v1} \\
\hat{z}_{v1}
\end{bmatrix}
=
\begin{bmatrix}
\cos\theta & \sin\theta\sin\phi & \sin\theta\cos\phi \\
0 & \cos\phi & -\sin\phi \\
-\sin\theta & \cos\theta\sin\phi & \cos\phi\cos\theta
\end{bmatrix}
\begin{bmatrix}
\hat{x}_b \\
\hat{y}_b \\
\hat{z}_b
\end{bmatrix}
\end{equation}

Proyección del eje $\hat{z}_{v1}$:
\begin{equation}
\hat{z}_{v1} = -\sin(\theta)\hat{x}_b + \cos(\theta)\sin(\phi)\hat{y}_b + \cos(\phi)\cos(\theta)\hat{z}_b
\end{equation}

\vspace{0.3cm}

% --- 3. Velocidad Angular ---
\subsection*{3. Vector de Velocidad Angular ($\boldsymbol{\omega}_{I}^b$)}

Suma de las velocidades angulares sobre sus respectivos ejes de rotación intermedios:
\begin{equation}
\begin{aligned}
\bm{\omega}_{I}^b &= \dot{\phi} \hat{x}_b 
+ \dot{\theta} \left( \cos\phi \hat{y}_b - \sin\phi \hat{z}_b \right) \\
&\quad + \dot{\psi} \left( -\sin\theta \hat{x}_b + \cos\theta\sin\phi \hat{y}_b + \cos\phi\cos\theta \hat{z}_b \right)
\end{aligned}
\end{equation}

Agrupando componentes por los ejes del cuerpo ($\hat{x}_b, \hat{y}_b, \hat{z}_b$):
\begin{equation}
\begin{aligned}
\bm{\omega}_{I}^b &= \left( \dot{\phi} - \dot{\psi}\sin\theta \right) \hat{x}_b \\
&\quad + \left( \dot{\theta}\cos\phi + \dot{\psi}\cos\theta\sin\phi \right) \hat{y}_b \\
&\quad + \left( -\dot{\theta}\sin\phi + \dot{\psi}\cos\phi\cos\theta \right) \hat{z}_b
\end{aligned}
\end{equation}

\vspace{0.3cm}

% --- 4. Relación de p, q, r ---
\subsection*{4. Ecuaciones Cinemáticas de Euler}

Componentes de la velocidad angular en los ejes fijos al cuerpo ($p, q, r$):
\begin{align}
p &= \dot{\phi} - \dot{\psi}\sin\theta \\
q &= \dot{\theta}\cos\phi + \dot{\psi}\cos\theta\sin\phi \\
r &= -\dot{\theta}\sin\phi + \dot{\psi}\cos\phi\cos\theta
\end{align}

Expresado en forma matricial:
\begin{equation}
\begin{bmatrix}
p \\
q \\
r
\end{bmatrix}
=
\begin{bmatrix}
1 & 0 & -\sin\theta \\
0 & \cos\phi & \cos\theta\sin\phi \\
0 & -\sin\phi & \cos\phi\cos\theta
\end{bmatrix}
\begin{bmatrix}
\dot{\phi} \\
\dot{\theta} \\
\dot{\psi}
\end{bmatrix}
\end{equation}

Por lo tanto, la tasa de variación de los ángulos de Euler es función de las velocidades angulares del cuerpo:
\begin{equation}
(\dot{\phi}, \dot{\theta}, \dot{\psi}) = f (p, q, r)
\end{equation}



\subsection{Dinámica traslacional en el marco del cuerpo}
\paragraph{Profe}
\subsubsection{Segunda ley de Newton}

\begin{equation}
 \left(m \frac{d\vec{V}}{dt} \right)_w = \sum \vec{F}
\end{equation}

Partiendo de la segunda ley de Newton en el marco rotante:

\begin{equation}
m \left( \frac{d\vec{V}}{dt} \right)_b + m \, \vec{\boldsymbol{\omega}} \times \vec{V}_b = \vec{F}
\end{equation}

Expresando en forma matricial:

\begin{equation}
m
\begin{bmatrix}
\dot{u} \\
\dot{v} \\
\dot{w}
\end{bmatrix}
+
m
\begin{bmatrix}
\hat{i} & \hat{j} & \hat{k} \\
p & q & r \\
u & v & w
\end{bmatrix}
=
\begin{bmatrix}
F_x \\
F_y \\
F_z
\end{bmatrix}
\end{equation}


\section*{Dinámica}
\paragraph{Javier Alejandro Tellez Paez}
Ecuación general de transporte (derivada de un vector en marco inercial vs.\ body):
\begin{equation}
\left(\frac{d\vec{A}}{dt}\right)_{I}=\left(\frac{d\vec{A}}{dt}\right)_{b}+\vec{\omega}\times\vec{A}
\end{equation}

Momento lineal $\vec{P}=m\vec{V}$ y momento angular $\vec{H}=\mathbf{I}\vec{\omega}$. Con $m$ constante:
\begin{align}
\frac{d\vec{P}}{dt}&=\frac{d(m\vec{V})}{dt}=m\,\frac{d\vec{V}}{dt}=\sum \vec{F}\\[4pt]
\frac{d(\mathbf{I}\vec{\omega})}{dt}&=\mathbf{I}\,\frac{d\vec{\omega}}{dt}=\sum \vec{\tau}
\end{align}

Aplicado al problema, con $(\cdot)_{I}$ marco inercial y $(\cdot)_{b}$ marco body:
\begin{align}
m\left(\frac{d\vec{V}}{dt}\right)_{I}&=\sum \vec{F}\\[4pt]
m\left[\left(\frac{d\vec{V}}{dt}\right)_{b}+\vec{\omega}\times\vec{V}_{b}\right]&=\sum \vec{F}
\end{align}

En componentes, con $\vec{\omega}=(p,q,r)$ y $\vec{V}_{b}=(u,v,w)$:
\begin{equation}
m\begin{bmatrix}\dot{u}\\[2pt]\dot{v}\\[2pt]\dot{w}\end{bmatrix}
+m\begin{vmatrix}\hat{\imath}&\hat{\jmath}&\hat{k}\\ p&q&r\\ u&v&w\end{vmatrix}
=\begin{bmatrix}F_{x}\\[2pt]F_{y}\\[2pt]F_{z}\end{bmatrix}
\end{equation}

Desarrollando el producto cruz:
\begin{equation}
m\begin{bmatrix}\dot{u}\\[2pt]\dot{v}\\[2pt]\dot{w}\end{bmatrix}
+m\begin{bmatrix}qw-rv\\[2pt]ru-pw\\[2pt]pv-qu\end{bmatrix}
=\begin{bmatrix}F_{x}\\[2pt]F_{y}\\[2pt]F_{z}\end{bmatrix}
\end{equation}

Despejando las aceleraciones:
\begin{equation}
\begin{bmatrix}\dot{u}\\[2pt]\dot{v}\\[2pt]\dot{w}\end{bmatrix}
=\frac{1}{m}\begin{bmatrix}F_{x}\\[2pt]F_{y}\\[2pt]F_{z}\end{bmatrix}
-\begin{bmatrix}qw-rv\\[2pt]ru-pw\\[2pt]pv-qu\end{bmatrix}
\end{equation}

Forma final para simulación:
\begin{equation}
\boxed{
\begin{bmatrix}\dot{u}\\[2pt]\dot{v}\\[2pt]\dot{w}\end{bmatrix}
=\frac{1}{m}\begin{bmatrix}F_{x}\\[2pt]F_{y}\\[2pt]F_{z}\end{bmatrix}
+\begin{bmatrix}rv-qw\\[2pt]pw-ru\\[2pt]qu-pv\end{bmatrix}}
\end{equation}

\paragraph{Laura Daniela Castaño Silva}
\textbf{Matriz de Asignación de Actuadores Mixed}

\subsection*{1. Vectores de Longitud y Empuje}

Primero, definimos los vectores de longitud (la posición de cada motor con respecto al centro del dron). Usamos la letra $l$ para representar esta distancia en los ejes X e Y:

\begin{center}
\begin{tikzpicture}[scale=1.5, >=stealth]
    % Ejes coordenados (xb, yb)
    \draw[dashed, thick] (0,0) -- (0,2.8) node[right] {$x_b$};
    \draw[dashed, thick] (0,0) -- (2.8,0) node[right] {$y_b$};

    % Brazos del dron
    \draw[very thick] (0,-2) -- (0,2);
    \draw[very thick] (-2,0) -- (2,0);

    % Distancias l (en color azul)
    \node[blue, right] at (0, 1) {$l$};
    \node[blue, right] at (0, -1) {$l$};
    \node[blue, above] at (1, 0) {$l$};
    \node[blue, above] at (-1, 0) {$l$};

    % Motores (círculos) y números
    \filldraw[fill=white, thick] (0,2) circle (0.15) node[gray, above=8pt] {\textbf{3}};
    \filldraw[fill=white, thick] (0,-2) circle (0.15) node[gray, below=8pt] {\textbf{4}};
    \filldraw[fill=white, thick] (2,0) circle (0.15) node[black, right=8pt] {\textbf{1}};
    \filldraw[fill=white, thick] (-2,0) circle (0.15) node[black, left=8pt] {\textbf{2}};

    % Flechas de rotación CW (magenta) - Motores 3 y 4
    \draw[->, thick, magenta] (-0.25, 2.15) arc[start angle=135, end angle=-45, radius=0.35];
    \node[magenta, right] at (0.3, 2.15) {CW};

    \draw[->, thick, magenta] (-0.25, -1.85) arc[start angle=135, end angle=-45, radius=0.35];
    \node[magenta, right] at (0.3, -1.85) {CW};

    % Flechas de rotación CCW (rojo) - Motores 1 y 2
    \draw[->, thick, red] (2.25, -0.15) arc[start angle=-45, end angle=135, radius=0.35];
    \node[red, above] at (2.15, 0.3) {CCW};

    \draw[->, thick, red] (-1.75, -0.15) arc[start angle=-45, end angle=135, radius=0.35];
    \node[red, above] at (-1.85, 0.3) {CCW};
\end{tikzpicture}
\end{center}

\begin{align*}
\vec{r}_{3} &= \begin{bmatrix} l \\ 0 \\ 0 \end{bmatrix}, & \vec{r}_{4} &= \begin{bmatrix} -l \\ 0 \\ 0 \end{bmatrix} \\
\vec{r}_{1} &= \begin{bmatrix} 0 \\ l \\ 0 \end{bmatrix}, & \vec{r}_{2} &= \begin{bmatrix} 0 \\ -l \\ 0 \end{bmatrix}
\end{align*}

Luego definimos el vector empuje o fuerza total. Como los motores apuntan hacia arriba, la fuerza va en el eje Z:
\begin{equation*}
\vec{F}_{k} = \begin{bmatrix} 0 \\ 0 \\ f_{t} \end{bmatrix}
\end{equation*}

\subsection*{2. Matrices de Torques (Eje Y - Inclinación / Cabeceo)}

Ahora calculamos las matrices de Torques respecto a la distancia y el empuje para los motores del eje X (motores 3 y 4). El torque ($\tau$) es el resultado del producto cruz entre el vector de posición y el de fuerza:

\begin{align*}
\tau_{3} &= \vec{r}_{3} \times \vec{F}_{t} \\
\tau_{4} &= \vec{r}_{4} \times \vec{F}_{t}
\end{align*}

Al realizar el producto cruz matemáticamente, la fuerza resultante recae sobre el eje Y:
\begin{align*}
\tau_{3} &= \begin{bmatrix} 0 \\ -l f_{t3} \\ 0 \end{bmatrix} \\
\tau_{4} &= \begin{bmatrix} 0 \\ l f_{t4} \\ 0 \end{bmatrix}
\end{align*}

Se suma el torque total para ese eje. Ahora, el torque que puede presentar inclinación $\theta$ respecto a $f_{t3}$ y $f_{t4}$ es:
\begin{align*}
T_{\theta} &= T_{y} = l f_{t3} - l f_{t4} \\
T_{y} &= l (f_{t3} - f_{t4})
\end{align*}

Sabemos que la fuerza de empuje de un motor se relaciona con la velocidad de su hélice ($\Omega$) mediante una constante $k_t$:
\begin{equation*}
f_{tk} = k_{t} \Omega_{k}^{2}
\end{equation*}

Reemplazando esto en nuestra ecuación de torque, obtenemos:
\begin{equation*}
T_{y} = l k_{t} (\Omega_{3}^{2} - \Omega_{4}^{2})
\end{equation*}

\subsection*{3. Matrices de Torques (Eje X - Alabeo)}

Hacemos exactamente el mismo análisis, pero para el eje X, usando los motores 1 y 2 que están en el eje Y:
\begin{align*}
T_{1} &= \vec{r}_{1} \times \vec{F}_{t} = \begin{bmatrix} l f_{t1} \\ 0 \\ 0 \end{bmatrix} \\
T_{2} &= \vec{r}_{2} \times \vec{F}_{t} = \begin{bmatrix} -l f_{t2} \\ 0 \\ 0 \end{bmatrix}
\end{align*}

Se analiza en el eje X, sumando ambos torques ($\tau_{x} = \tau_{1} + \tau_{2}$). El torque que puede presentar inclinación respecto a $f_{t1}$ y $f_{t2}$ es:
\begin{align*}
T_{\phi} &= T_{x} = l f_{t2} - l f_{t1} \\
T_{x} &= l (f_{t2} - f_{t1})
\end{align*}

Reemplazando nuevamente la fuerza por la velocidad angular al cuadrado, obtenemos:
\begin{equation*}
T_{x} = l k_{t} (\Omega_{2}^{2} - \Omega_{1}^{2})
\end{equation*}

\vspace{0.5cm}
\textbf{Nota sobre la constante de empuje:} Teniendo en cuenta la aerodinámica de las hélices, la constante $k_t$ se define como:
\begin{equation*}
K_{t} = C_{l} \rho A
\end{equation*}
Donde:
\begin{itemize}
    \item $C_{l} = \text{Coeficiente de empuje}$
    \item $\rho = \text{Densidad del aire}$
    \item $A = \text{Área de hélice}$
\end{itemize}

\section*{Gráficas}
\paragraph{Ma Sofia Esguerra G cod 1804228}

\begin{figure}
\centering

\begin{tikzpicture}[
    >=Latex,
    axis/.style={->, thick},
    bodyaxis/.style={->, very thick},
    anglearc/.style={->, thick, dashed},
    every node/.style={font=\small}
]

% =========================================================
% ORIGEN
% =========================================================
\coordinate (O) at (0,0);

% =========================================================
% EJES INERCIALES
% =========================================================

% X
\draw[axis] (O) -- (-2,-1.7)
    node[below left] {$X_I$};

% Y
\draw[axis] (O) -- (3,0)
    node[right] {$Y_I$};

% Z positivo hacia abajo
\draw[axis] (O) -- (0,-3)
    node[below] {$Z_I$};
    
\node[blue, left] at (0,-1.1) {$Z_I(+)$};

% =========================================================
% EJES BODY DESPUÉS DEL YAW
% =========================================================

% X_B
\draw[bodyaxis, red]
    (O) -- (2.5,-1.8)
    node[right] {$X_B$};

% Y_B
\draw[bodyaxis, red]
    (O) -- (2.1,1.2)
    node[above right] {$Y_B$};

% =========================================================
% ARCO DE YAW
% =========================================================

\draw[anglearc, blue]
    (1.0,0)
    arc[start angle=0,end angle=-36,radius=1.0];

\node[blue] at (1.15,-0.45) {$\psi$};

% =========================================================
% ORIGEN
% =========================================================

\filldraw[black] (O) circle (2pt);

% =========================================================
% TEXTO
% =========================================================

\node[draw, rounded corners, red, align=center]
    at (4,-1.8)
    {$\psi$: Yaw\\[2mm]
     Rotación alrededor de $Z$};

\node[draw, blue, align=center]
    at (0,-4)
    {$Z$ positivo hacia ABAJO};

\end{tikzpicture}

\caption{Rotación alrededor del eje $Z$ — Yaw.}
\label{fig:yaw}

\end{figure}

\begin{figure}
\centering

\begin{tikzpicture}[
    >=Latex,
    axis/.style={->, thick},
    bodyaxis/.style={->, very thick},
    anglearc/.style={->, thick, dashed},
    every node/.style={font=\small}
]

\coordinate (O) at (0,0);

% =========================================================
% EJES INERCIALES
% =========================================================

% X
\draw[axis]
    (O) -- (-2.2,-1.6)
    node[below left] {$X_I$};

% Y
\draw[axis]
    (O) -- (3,0)
    node[right] {$Y_I$};

% Z positivo hacia abajo
\draw[axis]
    (O) -- (0,-3)
    node[below] {$Z_I$};

\node[blue, left] at (0,-1.1) {$Z_I(+)$};

% =========================================================
% EJES BODY
% =========================================================

% X_B rotado
\draw[bodyaxis, green!60!black]
    (O) -- (-1.8,0.9)
    node[above left] {$X_B$};

% Z_B rotado
\draw[bodyaxis, green!60!black]
    (O) -- (2.0,-2.1)
    node[right] {$Z_B$};

% =========================================================
% EJE Y DE ROTACIÓN
% =========================================================

\draw[very thick, gray]
    (O) -- (0,2.4)
    node[above] {$Y$};

% =========================================================
% ARCO PITCH
% =========================================================

\draw[anglearc, green!60!black]
    (-0.8,-0.7)
    arc[start angle=220,end angle=315,radius=1.0];

\node[green!60!black] at (0.0,-1.2) {$\theta$};

% =========================================================
% ORIGEN
% =========================================================

\filldraw[black] (O) circle (2pt);

% =========================================================
% TEXTO
% =========================================================

\node[
    draw,
    rounded corners,
    green!50!black,
    align=center
]
at (4,-1.8)
{$\theta$: Pitch\\[2mm]
Rotación alrededor de $Y$};

\node[draw, blue, align=center]
at (0,-4)
{$Z$ positivo hacia ABAJO};

\end{tikzpicture}

\caption{Rotación alrededor del eje $Y$ — Pitch.}
\label{fig:pitch}

\end{figure}

\begin{figure}
\centering

\begin{tikzpicture}[
    >=Latex,
    axis/.style={->, thick},
    bodyaxis/.style={->, very thick},
    anglearc/.style={->, thick, dashed},
    every node/.style={font=\small}
]

\coordinate (O) at (0,0);

% =========================================================
% EJES INERCIALES
% =========================================================

% X
\draw[axis]
    (O) -- (-2.2,-1.6)
    node[below left] {$X_I$};

% Y
\draw[axis]
    (O) -- (3,0)
    node[right] {$Y_I$};

% Z positivo hacia abajo
\draw[axis]
    (O) -- (0,-3)
    node[below] {$Z_I$};

\node[blue, left] at (0,-1.1) {$Z_I(+)$};

% =========================================================
% EJE X DE ROTACIÓN
% =========================================================

\draw[very thick, gray]
    (O) -- (-2.2,-1.6);

\node[gray] at (-1.5,-1.0) {$X$};

% =========================================================
% EJES BODY
% =========================================================

% Y_B
\draw[bodyaxis, blue]
    (O) -- (2.5,1.3)
    node[above right] {$Y_B$};

% Z_B
\draw[bodyaxis, blue]
    (O) -- (2.1,-2.2)
    node[right] {$Z_B$};

% =========================================================
% ARCO ROLL
% =========================================================

\draw[anglearc, blue]
    (1.1,0)
    arc[start angle=0,end angle=-47,radius=1.1];

\node[blue] at (1.3,-0.55) {$\phi$};

% =========================================================
% ORIGEN
% =========================================================

\filldraw[black] (O) circle (2pt);

% =========================================================
% TEXTO
% =========================================================

\node[
    draw,
    rounded corners,
    blue,
    align=center
]
at (4,-1.8)
{$\phi$: Roll\\[2mm]
Rotación alrededor de $X$};

\node[draw, blue, align=center]
at (0,-4)
{$Z$ positivo hacia ABAJO};

\end{tikzpicture}

\caption{Rotación alrededor del eje $X$ — Roll.}
\label{fig:roll}

\end{figure}

\begin{figure}
\centering

\begin{tikzpicture}[
    >=Latex,
    axis/.style={->, very thick},
    bodyaxis/.style={->, ultra thick},
    every node/.style={font=\small}
]

% =========================================================
% ORIGEN DEL SISTEMA INERCIAL
% =========================================================

\coordinate (O) at (-3,0);

% =========================================================
% MARCO INERCIAL NED
% =========================================================

% PN - Norte
\draw[axis, red]
    (O) -- (0,0)
    node[midway, above] {$P_N$};

% PE - Este
\draw[axis, green!60!black]
    (O) -- (-3,2.2)
    node[above] {$P_E$};

% PD - Down
\draw[axis, blue]
    (O) -- (-3,-2.8)
    node[below] {$P_D$};

% =========================================================
% ORIGEN
% =========================================================

\filldraw[black] (O) circle (3pt);

% =========================================================
% MARCO BODY
% =========================================================

\coordinate (B) at (4,0.5);

% xB
\draw[bodyaxis, red]
    (B) -- (6.5,1.8)
    node[right] {$x_B$};

% yB
\draw[bodyaxis, green!60!black]
    (B) -- (2.0,1.2)
    node[left] {$y_B$};

% zB
\draw[bodyaxis, blue]
    (B) -- (4,-2.2)
    node[right] {$z_B$};

% =========================================================
% CUERPO
% =========================================================

\draw[thick]
    (3.5,0.2) --
    (4.7,0.8) --
    (5.2,0.4) --
    (4.0,-0.1) --
    cycle;

\draw[thick]
    (3.5,0.2) --
    (4.0,-0.1);

\draw[thick]
    (4.7,0.8) --
    (5.2,0.4);

% =========================================================
% YAW
% =========================================================

\draw[
    ->,
    very thick,
    blue,
    dashed
]
(0.2,0)
arc[start angle=0,end angle=25,radius=4.2];

\node[blue]
at (0.7,0.8)
{$\psi$};

\node[blue]
at (1.0,1.2)
{Yaw};

% =========================================================
% PITCH
% =========================================================

\draw[
    ->,
    very thick,
    green!60!black
]
(4.0,1.2)
arc[start angle=90,end angle=35,radius=0.8];

\node[green!50!black]
at (4.5,1.4)
{$\theta$};

\node[green!50!black]
at (5.0,1.7)
{Pitch};

% =========================================================
% ROLL
% =========================================================

\draw[
    ->,
    very thick,
    red
]
(4.7,0.2)
arc[start angle=-20,end angle=35,radius=0.8];

\node[red]
at (5.4,0.0)
{$\phi$};

\node[red]
at (5.8,-0.4)
{Roll};

% =========================================================
% ETIQUETAS DE MARCOS
% =========================================================

\node[
    draw,
    rounded corners,
    blue,
    fill=white
]
at (-3,3.0)
{\textbf{Marco Inercial (NED)}};

\node[
    draw,
    rounded corners,
    blue,
    fill=white
]
at (4,3.0)
{\textbf{Marco Body (B)}};

% =========================================================
% CONVENCIÓN
% =========================================================

\node[
    draw,
    rounded corners,
    align=left,
    fill=white
]
at (-0.2,-3.5)
{
\textbf{Convención NED:}\\
$P_N$: Norte\\
$P_E$: Este\\
$P_D$: Down $\rightarrow$ positivo hacia abajo
};

% =========================================================
% ÁNGULOS DE EULER
% =========================================================

\node[
    draw,
    rounded corners,
    align=left,
    fill=white
]
at (7.5,-1.5)
{
\textbf{Ángulos de Euler}\\[1mm]
$\psi$: Yaw $\rightarrow Z$\\
$\theta$: Pitch $\rightarrow Y$\\
$\phi$: Roll $\rightarrow X$
};

\end{tikzpicture}

\caption{
Marco de referencia inercial NED y marco Body con los ángulos de Euler.
}
\label{fig:frames}
\end{figure}

 
\section{Modelo de Sensores Inerciales Densidad Espectral de Potencia (PSD)}
 \paragraph{Lopez Michell}
El modelo matemático que describe la medición de un acelerómetro está dado por:
 
\begin{equation}
    a_m = M_a \cdot a + b_a + n_a
\end{equation}
 
Donde cada término representa lo siguiente:
 
\begin{itemize}
    \item $a_m$: Aceleración medida.
    \item $M_a$: Matriz que representa la ganancia, desalineamiento entre ejes y escala.
    \item $a$: Aceleración real que mide el acelerómetro.
    \item $b_a$: Bias o sesgo del acelerómetro.
    \item $n_a$: Ruido de la medición.
\end{itemize}
 
\subsection{Ruido de la medición}
 
El ruido de la medición $n_a$ se modela como una variable aleatoria con distribución normal de media cero y varianza $\sigma^2$:
 
\begin{equation}
    n_a \sim N(0, \sigma^2)
\end{equation}
 
A continuación se muestra la representación gráfica de la distribución normal y su Densidad Espectral de Potencia (PSD) correspondiente:
 
\begin{center}
% --- Campana de Gauss ---
\begin{tikzpicture}[scale=1]
    \node at (0,2.1) {$N(0,\sigma^2)$};
    % Ejes
    \draw[-latex] (-2.5,0) -- (2.5,0);
    \draw[-latex] (0,0) -- (0,2);
    % Curva gaussiana
    \draw[thick, purple, domain=-2.2:2.2, samples=80, smooth]
        plot (\x, {1.8*exp(-\x*\x/2)});
    % Marcas en el eje x
    \foreach \x/\lbl in {-2/-2\sigma, -1/-\sigma, 0/0, 1/\sigma, 2/2\sigma} {
        \draw (\x,0.05) -- (\x,-0.05) node[below] {\footnotesize $\lbl$};
    }
\end{tikzpicture}
\hspace{1.5cm}
% --- PSD constante ---
\begin{tikzpicture}[scale=1]
    \node at (0,2.1) {PSD};
    % Ejes
    \draw[-latex] (-2.5,0) -- (2.5,0) node[right] {$\omega$};
    \draw (-2.3,0) node[below] {\footnotesize $-\omega$};
    \draw[-latex] (0,0) -- (0,2);
    % Linea PSD constante, tipo onda seno
    \draw[thick, purple, domain=-2.3:2.3, samples=150, smooth]
        plot (\x, {1 + 0.06*sin(deg(6*\x))});
\end{tikzpicture}
\end{center}
 
La Densidad Espectral de Potencia (PSD) de este tipo de ruido es \textbf{constante} en todo el espectro de frecuencias, y sus unidades están dadas por:
 
\begin{equation}
    PSD = \text{Constante} \; \left[ \dfrac{(\text{Unidades})^2}{Hz} \right]
\end{equation}
 
\newpage
 
\section{Ejemplo: Sensor MPU-6050}
 
 
\subsection{Densidad de ruido (Noise Density)}
 
\begin{equation}
    N_a = 400 \; \dfrac{\mu g}{\sqrt{Hz}}
\end{equation}
 
Convirtiendo las unidades de $\mu g$ a $m/s^2$ (multiplicando por $9.81 \; m/s^2$):
 
\begin{equation}
    N_a = \dfrac{400 \times 10^{-6} \cdot 9.81 \; m/s^2}{\sqrt{Hz}}
\end{equation}
 
\begin{equation}
    N_a = 3.924 \times 10^{-3} \; \dfrac{m/s^2}{\sqrt{Hz}}
\end{equation}
 
\subsection{Cálculo de la PSD}
 
Sabiendo que la relación entre la densidad de ruido y la PSD es:
 
\begin{equation}
    PSD = N_a^2
\end{equation}
 
Sustituyendo el valor obtenido:
 
\begin{equation}
    PSD = 1.539 \times 10^{-5} \; \dfrac{(m/s^2)^2}{Hz}
\end{equation}
 
\subsection{Cálculo de la varianza}
 
La varianza del ruido se relaciona con la PSD y la frecuencia de muestreo $f_s$ mediante:
 
\begin{equation}
    \sigma^2 = PSD \cdot f_s \quad \text{(} f_s \text{: frecuencia de muestreo)}
\end{equation}
 
Tomando $f_s = 1 \times 10^{3} \; Hz$ como frecuencia de muestreo:
 
\begin{equation}
    \sigma^2 = 1.539 \times 10^{-5} \cdot (1 \times 10^{3})
\end{equation}
 
\begin{equation}
    \boxed{\sigma^2 = 0.01539}
\end{equation}

\section{Plantas en lazo abierto}
% ======================================================================

A partir del modelo linealizado alrededor del vuelo estacionario, los cuatro
canales quedan desacoplados y cada uno se reduce a un doble integrador:
\begin{equation}
\frac{\Phi(s)}{T_\phi(s)}=\frac{1/I_{xx}}{s^{2}},\qquad
\frac{\Theta(s)}{T_\theta(s)}=\frac{1/I_{yy}}{s^{2}},\qquad
\frac{\Psi(s)}{T_\psi(s)}=\frac{1/I_{zz}}{s^{2}},\qquad
\frac{P_N(s)}{F_t(s)}=\frac{1/m}{s^{2}} .
\label{eq:plantas}
\end{equation}

Las cuatro plantas comparten la misma estructura, por lo que se define la
\textbf{planta genérica}
\begin{equation}
G(s)=\frac{Y(s)}{U(s)}=\frac{1/J}{s^{2}},
\qquad
J \in \{\,I_{xx},\;I_{yy},\;I_{zz},\;m\,\},\quad
Y \in \{\,\Phi,\;\Theta,\;\Psi,\;P_N\,\},
\label{eq:generica}
\end{equation}
donde $J$ es el parámetro inercial del canal. Todo el desarrollo se realiza una
sola vez sobre \eqref{eq:generica} y luego se particulariza.

% ======================================================================
\section{Ley de control}
% ======================================================================

De acuerdo con el diagrama de bloques planteado, la ley de control es
\begin{equation}
u(t) = K_I \int_{0}^{t} e(\tau)\,d\tau \;-\; K_P\, y(t) \;-\; K_D\, \dot y(t),
\qquad e(t)=r(t)-y(t).
\label{eq:leycontrol}
\end{equation}

La acción integral actúa sobre el error $e=r-y$, mientras que las acciones
proporcional y derivativa actúan sobre la salida $y$. Esta configuración se
denomina \emph{PI--D}.

\begin{figure}[h!]
\centering
\begin{tikzpicture}[auto, node distance=1.5cm, >=Latex, thick]

  \node [input, name=input] {};
  \node [sum, right=1.4cm of input] (sum1) {};
  \node [block, right=1.2cm of sum1] (ki) {$K_I$};
  \node [block, right=1.2cm of ki] (int) {$\dfrac{1}{s}$};
  \node [sum, right=1.6cm of int] (sum2) {};
  \node [block, right=1.4cm of sum2, minimum width=3.2em] (plant) {$\dfrac{1/J}{s^{2}}$};
  \node [coordinate, right=1.0cm of plant] (fb) {};
  \node [output, right=1.0cm of fb] (output) {};

  \node [block, below=1.8cm of ki] (kp) {$K_P$};
  \node [block, below=1.8cm of kp] (kd) {$K_D$};
  \node [block, right=1.2cm of kd] (der) {$s$};

  % camino directo
  \draw [->] (input) -- node[pos=0.15,above] {$R(s)$} node[pos=0.85,above] {\small $+$} (sum1);
  \draw [->] (sum1) -- node[above] {\small $E(s)$} (ki);
  \draw [->] (ki) -- (int);
  \draw [->] (int) -- node[pos=0.85,above] {\small $+$} (sum2);
  \draw [->] (sum2) -- node[above] {$U(s)$} (plant);
  \draw [-]  (plant) -- (fb);
  \draw [->] (fb) -- node[above,pos=0.9] {$Y(s)$} (output);
  \node [punto] at (fb) {};

  % realimentación principal
  \draw [-]  (fb) -- ++(0,-5.9) coordinate (bottom);
  \draw [->] (bottom) -| node[pos=0.93,left] {\small $-$} (sum1);

  % ramas P y D
  \node [punto] at (sum1 |- kp) {};
  \node [punto] at (sum1 |- kd) {};
  \draw [->] (sum1 |- kp) -- (kp);
  \draw [->] (sum1 |- kd) -- (kd);
  \draw [->] (kd) -- (der);
  \draw [->] (kp.east) -| node[pos=0.93,left] {\small $-$} (sum2.south);
  \draw [->] (der.east) -| node[pos=0.93,right] {\small $-$} (sum2.south east);

\end{tikzpicture}
\caption{Estructura PI--D aplicada a la planta genérica $G(s)=\dfrac{1/J}{s^{2}}$.
Para el canal de alabeo, $J=I_{xx}$, $Y=\Phi$ y $R$ es la referencia de alabeo.}
\label{fig:bloques}
\end{figure}

% ======================================================================
\section{Función de transferencia en lazo cerrado}
\label{sec:lazocerrado Santiago}
% ======================================================================

\paragraph{Paso 1: transformada de Laplace de la ley de control.}
Aplicando $\Lap\{\cdot\}$ a \eqref{eq:leycontrol} con condiciones iniciales
nulas, y usando $\Lap\{\int_0^t e\} = E(s)/s$ y $\Lap\{\dot y\}=sY(s)$:
\begin{equation}
U(s)=\frac{K_I}{s}\,E(s)-K_P\,Y(s)-K_D\,s\,Y(s).
\end{equation}

\paragraph{Paso 2: sustitución del error.}
Con $E(s)=R(s)-Y(s)$,
\begin{equation}
U(s)=\frac{K_I}{s}\big[R(s)-Y(s)\big]-K_P\,Y(s)-K_D\,s\,Y(s).
\label{eq:paso2}
\end{equation}

\paragraph{Paso 3: ecuación de la planta.}
De \eqref{eq:generica},
\begin{equation}
Y(s)=\frac{1/J}{s^{2}}\,U(s)
\qquad\Longleftrightarrow\qquad
U(s)=J\,s^{2}\,Y(s).
\label{eq:paso3}
\end{equation}

\paragraph{Paso 4: igualación.}
Sustituyendo \eqref{eq:paso3} en \eqref{eq:paso2}:
\begin{equation}
J\,s^{2}\,Y(s)=\frac{K_I}{s}R(s)-\frac{K_I}{s}Y(s)-K_P\,Y(s)-K_D\,s\,Y(s).
\end{equation}

\paragraph{Paso 5: eliminación de denominadores.}
Multiplicando toda la ecuación por $s$:
\begin{equation}
J\,s^{3}\,Y(s)=K_I\,R(s)-K_I\,Y(s)-K_P\,s\,Y(s)-K_D\,s^{2}\,Y(s).
\end{equation}

\paragraph{Paso 6: agrupación de términos en $Y(s)$.}
\begin{equation}
Y(s)\Big[J\,s^{3}+K_D\,s^{2}+K_P\,s+K_I\Big]=K_I\,R(s).
\end{equation}

\paragraph{Paso 7: función de transferencia.}
\begin{equation}
\boxed{\;
\frac{Y(s)}{R(s)}=\frac{K_I}{J\,s^{3}+K_D\,s^{2}+K_P\,s+K_I}
=\frac{K_I/J}{s^{3}+\dfrac{K_D}{J}s^{2}+\dfrac{K_P}{J}s+\dfrac{K_I}{J}} \;}
\label{eq:lazocerrado}
\end{equation}

El lazo cerrado resulta de tercer orden: los dos polos de la planta más el polo
aportado por el integrador.

\subsection{Particularización a los cuatro canales}

Reemplazando $J$ y $Y$ en \eqref{eq:lazocerrado} se obtienen directamente las
cuatro funciones de transferencia.

\paragraph{Alabeo (\emph{roll}), $J=I_{xx}$:}
\begin{equation}
\boxed{\;\frac{\Phi(s)}{R_\phi(s)}
=\frac{K_{I\phi}}{I_{xx}s^{3}+K_{D\phi}s^{2}+K_{P\phi}s+K_{I\phi}}\;}
\end{equation}

\paragraph{Cabeceo (\emph{pitch}), $J=I_{yy}$:}
\begin{equation}
\boxed{\;\frac{\Theta(s)}{R_\theta(s)}
=\frac{K_{I\theta}}{I_{yy}s^{3}+K_{D\theta}s^{2}+K_{P\theta}s+K_{I\theta}}\;}
\end{equation}

\paragraph{Guiñada (\emph{yaw}), $J=I_{zz}$:}
\begin{equation}
\boxed{\;\frac{\Psi(s)}{R_\psi(s)}
=\frac{K_{I\psi}}{I_{zz}s^{3}+K_{D\psi}s^{2}+K_{P\psi}s+K_{I\psi}}\;}
\end{equation}

\paragraph{Posición norte, $J=m$:}
\begin{equation}
\boxed{\;\frac{P_N(s)}{R_{p}(s)}
=\frac{K_{Ip}}{m\,s^{3}+K_{Dp}s^{2}+K_{Pp}s+K_{Ip}}\;}
\end{equation}

Puesto que en un cuadricóptero simétrico $I_{xx}=I_{yy}$, los canales de alabeo
y cabeceo son idénticos y comparten la misma sintonización.

% ======================================================================
\section{Diseño por asignación de polos}
% ======================================================================

\subsection{Especificaciones de diseño}

Se adopta un amortiguamiento $\zeta=0.9$ para todos los canales, de modo que la
respuesta sea prácticamente sin oscilación. El tiempo de establecimiento se fija
según una jerarquía de lazos: el lazo interno de actitud debe ser el más rápido,
la guiñada intermedia y el lazo externo de posición el más lento.

\begin{center}
\begin{tabular}{lcc}
\toprule
Canal & $\zeta$ & $t_{ss}$ [s] \\
\midrule
Alabeo $\phi$ y cabeceo $\theta$ & 0.9 & 0.5 \\
Guiñada $\psi$                   & 0.9 & 1.0 \\
Posición $p_N$                   & 0.9 & 2.0 \\
\bottomrule
\end{tabular}
\end{center}

Con el criterio del 2%,
\begin{equation}
t_{ss}\approx\frac{4}{\zeta\omega_n}
\qquad\Longrightarrow\qquad
\omega_n=\frac{4}{\zeta\,t_{ss}} .
\label{eq:wn}
\end{equation}

El tercer polo se ubica en $p=5\zeta\omega_n$, suficientemente alejado del par
dominante para que su efecto transitorio sea despreciable.

\subsection{Polinomio característico deseado e igualación}

Los polos deseados corresponden a un par complejo conjugado dominante y un polo
real:
\begin{equation}
\Delta_d(s)=\big(s^{2}+2\zeta\omega_n s+\omega_n^{2}\big)\big(s+p\big).
\end{equation}

Desarrollando el producto:
\begin{align}
\Delta_d(s)&=s^{3}+2\zeta\omega_n s^{2}+\omega_n^{2}s
            +p\,s^{2}+2\zeta\omega_n p\,s+\omega_n^{2}p \nonumber\\[2pt]
&=s^{3}+\big(2\zeta\omega_n+p\big)s^{2}
       +\big(\omega_n^{2}+2\zeta\omega_n p\big)s
       +\omega_n^{2}p .
\label{eq:deltad}
\end{align}

El polinomio característico del lazo cerrado, tomado del denominador de
\eqref{eq:lazocerrado} en forma mónica, es
\begin{equation}
\Delta(s)=s^{3}+\frac{K_D}{J}s^{2}+\frac{K_P}{J}s+\frac{K_I}{J}.
\label{eq:delta}
\end{equation}

Igualando \eqref{eq:delta} con \eqref{eq:deltad} coeficiente a coeficiente:
\begin{equation}
\frac{K_D}{J}=2\zeta\omega_n+p,\qquad
\frac{K_P}{J}=\omega_n^{2}+2\zeta\omega_n p,\qquad
\frac{K_I}{J}=\omega_n^{2}p ,
\end{equation}
y despejando las ganancias:
\begin{equation}
\boxed{\;
K_D=J\big(2\zeta\omega_n+p\big),\qquad
K_P=J\big(\omega_n^{2}+2\zeta\omega_n p\big),\qquad
K_I=J\,\omega_n^{2}\,p \;}
\label{eq:ganancias}
\end{equation}

Las tres ganancias resultan proporcionales al parámetro inercial $J$ del canal.

\subsection{Aplicación a cada canal}

Evaluando \eqref{eq:wn} con $\zeta=0.9$ y $p=5\zeta\omega_n$:

\begin{center}
\begin{tabular}{lcccc}
\toprule
Canal & $t_{ss}$ [s] & $\omega_n$ [rad/s] & $\zeta\omega_n$ & $p$ [rad/s]\\
\midrule
$\phi,\ \theta$ & 0.5 & 8.889 & 8 & 40 \\
$\psi$          & 1.0 & 4.444 & 4 & 20 \\
$p_N$           & 2.0 & 2.222 & 2 & 10 \\
\bottomrule
\end{tabular}
\end{center}

Sustituyendo estos valores en \eqref{eq:ganancias} se obtienen las ganancias en
forma simbólica:

\begin{center}
\begin{tabular}{lccc}
\toprule
Canal & $K_P$ & $K_I$ & $K_D$ \\
\midrule
$\phi$   & $719.01\,I_{xx}$ & $3160.49\,I_{xx}$ & $56\,I_{xx}$ \\
$\theta$ & $719.01\,I_{yy}$ & $3160.49\,I_{yy}$ & $56\,I_{yy}$ \\
$\psi$   & $179.75\,I_{zz}$ & $\phantom{0}395.06\,I_{zz}$ & $28\,I_{zz}$ \\
$p_N$    & $\phantom{0}44.94\,m$ & $\phantom{00}49.38\,m$ & $14\,m$ \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Valores numéricos}

Empleando los momentos de inercia deducidos en la sección de modelado,
\begin{equation}
I_{xx}=I_{yy}=\frac{2MR^{2}}{5}+2\ell^{2}m_r,
\qquad
I_{zz}=\frac{2MR^{2}}{5}+4\ell^{2}m_r,
\qquad
m=M+4m_r ,
\end{equation}
con $M=\SI{1.0}{\kilogram}$, $R=\SI{0.05}{\meter}$, $\ell=\SI{0.225}{\meter}$ y
$m_r=\SI{0.06}{\kilogram}$:
\begin{equation}
I_{xx}=I_{yy}=\SI{7.075e-3}{\kilogram\meter\squared},\qquad
I_{zz}=\SI{1.315e-2}{\kilogram\meter\squared},\qquad
m=\SI{1.24}{\kilogram}.
\end{equation}

\begin{center}
\begin{tabular}{lcccc}
\toprule
Canal & $J$ & $K_P$ & $K_I$ & $K_D$ \\
\midrule
$\phi$   & \num{7.075e-3} & \num{5.0870}  & \num{22.3605} & \num{0.39620} \\
$\theta$ & \num{7.075e-3} & \num{5.0870}  & \num{22.3605} & \num{0.39620} \\
$\psi$   & \num{1.315e-2} & \num{2.3638}  & \num{5.1951}  & \num{0.36820} \\
$p_N$    & \num{1.24}     & \num{55.7235} & \num{61.2346} & \num{17.3600} \\
\bottomrule
\end{tabular}
\end{center}

Las funciones de transferencia en lazo cerrado quedan entonces
\begin{align}
\frac{\Phi(s)}{R_\phi(s)}=\frac{\Theta(s)}{R_\theta(s)}
&=\frac{3160.5}{s^{3}+56\,s^{2}+719.0\,s+3160.5},\\[6pt]
\frac{\Psi(s)}{R_\psi(s)}
&=\frac{395.06}{s^{3}+28\,s^{2}+179.75\,s+395.06},\\[6pt]
\frac{P_N(s)}{R_p(s)}
&=\frac{49.383}{s^{3}+14\,s^{2}+44.938\,s+49.383},
\end{align}
cuyos polos son, por construcción, los asignados:

\begin{center}
\begin{tabular}{lcc}
\toprule
Canal & Par dominante & Polo real \\
\midrule
$\phi,\ \theta$ & $-8 \pm j3.874$ & $-40$ \\
$\psi$          & $-4 \pm j1.937$ & $-20$ \\
$p_N$           & $-2 \pm j0.968$ & $-10$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent
donde la parte imaginaria corresponde a
$\omega_d=\omega_n\sqrt{1-\zeta^{2}}$.


\paragraph{Angelica Gabriela Gomez}
Relación entre aceleraciones y matriz de rotación
\begin{equation}
\begin{bmatrix}
a_{xb} \\
a_{yb} \\
a_{zb}
\end{bmatrix}
=
R_I^b
\begin{bmatrix}
0 \\
0 \\
g
\end{bmatrix}
\end{equation}
 Componente $x$
\begin{equation}
a_{xb} = g\sin(\theta)
\end{equation}
Componente $y$
\begin{equation}
a_{yb} = -g\cos(\theta)\sin(\phi)
\end{equation}
Componente $z$
\begin{equation}
a_{zb} = -g\cos(\theta)\cos(\phi)
\end{equation}
\begin{equation}
-g\cos(\theta)
=
\frac{a_{yb}}{\sin(\phi)}
\end{equation}
\begin{equation}
-g\cos(\theta)
=
\frac{a_{zb}}{\cos(\phi)}
\end{equation}
Relación entre componentes
\begin{equation}
\frac{a_{yb}}{\sin(\phi)}
=
\frac{a_{zb}}{\cos(\phi)}
\end{equation}
Relación trigonométrica
\begin{equation}
\frac{\sin(\phi)}{\cos(\phi)}
=
\frac{a_{yb}}{a_{zb}}
\end{equation}
Estimación de $\phi$ mediante el acelerómetro
\begin{equation}
\dot{\phi}_{\mathrm{accel}}
=
\tan^{-1}
\left(
\frac{a_{yb}}{a_{zb}}
\right)
\end{equation}
Estimación de $\theta$ mediante el acelerómetro
\begin{equation}
\dot{\theta}_{\mathrm{accel}}
=
\sin^{-1}
\left(
\frac{a_{xb}}{g}
\right)
\end{equation}
Relación entre velocidades angulares y ángulos
\begin{equation}
\begin{bmatrix}
\dot{\phi} \\
\dot{\theta}
\end{bmatrix}
=
\begin{bmatrix}
1 & \sin(\phi)\tan(\theta) & \cos(\phi)\tan(\theta) \\
0 & \cos(\phi) & -\sin(\phi)
\end{bmatrix}
\begin{bmatrix}
p \\
q \\
r
\end{bmatrix}
\end{equation}
Velocidad angular de $\phi$
\begin{equation}
\dot{\phi}_{k}
=
p
+
\sin(\phi)\tan(\theta)q
+
\cos(\phi)\tan(\theta)r
\end{equation}
Velocidad angular de $\theta$
\begin{equation}
\dot{\theta}_{k}
=
\cos(\phi)q
-
\sin(\phi)r
\end{equation}
Actualización de $\phi$
\begin{equation}
\phi_{k+1}
=
\phi_k
+
T_s\dot{\phi}_k
\end{equation}
Actualización de $\phi$ expandida
\begin{equation}
\phi_{k+1}
=
\phi_k
+
T_s
\left[
p
+
\sin(\phi)\tan(\theta)q
+
\cos(\phi)\tan(\theta)r
\right]
\end{equation}
Actualización de $\theta$
\begin{equation}
\theta_{k+1}
=
\theta_k
+
T_s\dot{\theta}_k
\end{equation}
 Modelo de espacio de estados no lineal
\begin{equation}
x_{k+1}
=
f(x_k,u_k)
\end{equation}
Modelo linealizado
\begin{equation}
x_{k+1}
=
Ax_k
+
Bu_k
\end{equation}
Matriz de transición $\Phi_k$
\begin{equation}
\Phi_k
=
\begin{bmatrix}
\dfrac{\partial\phi_{k+1}}{\partial\phi_k} &
\dfrac{\partial\phi_{k+1}}{\partial\theta_k} \\[8pt]
\dfrac{\partial\theta_{k+1}}{\partial\phi_k} &
\dfrac{\partial\theta_{k+1}}{\partial\theta_k}
\end{bmatrix}
\end{equation}
 Matriz de observación
\begin{equation}
H_k
=
\begin{bmatrix}
1 & 0 \\
0 & 1
\end{bmatrix}
\end{equation}
Ganancia de Kalman
\begin{equation}
K_k
=
P_{k^-}H_k^T
\left(
H_kP_{k^-}H_k^T+R_k
\right)^{-1}
\end{equation}
Actualización del estado
\begin{equation}
X_k
=
X_{k^-}
+
K_k
\left(
Z_k-H_kX_{k^-}
\right)
\end{equation}
 Matriz de covarianza del ruido del proceso
\begin{equation}
Q
=
\begin{bmatrix}
\sigma_{\phi_{\mathrm{gyr}}}^{2} & 0 \\
0 & \sigma_{\theta_{\mathrm{gyr}}}^{2}
\end{bmatrix}
\end{equation}
 Actualización de la matriz de covarianza
\begin{equation}
P_k
=
\left(
I-K_kH_k
\right)P_{k^-}
\end{equation}
 Matriz de covarianza del ruido de medición
\begin{equation}
R
=
\begin{bmatrix}
\sigma_{\phi_{\mathrm{accel}}}^{2} & 0 \\
0 & \sigma_{\theta_{\mathrm{accel}}}^{2}
\end{bmatrix}
\end{equation}
 Modelo de transición
\begin{equation}
x_{k+1}
=
f(x_k,u_k)
\end{equation}
 Predicción de la covarianza
\begin{equation}
P_{k+1}
=
\Phi_kP_k\Phi_k^T
+
Q_k
\end{equation}

\paragraph{Juan Camilo Blanco}

\section{Matriz de asignación de actuadores en dron tipo Quad X}

Los vectores de posición de los cuatro rotores respecto al centro de masa del dron son:

\begin{equation}
\mathbf{r}_1 =
\begin{bmatrix}
\dfrac{L}{\sqrt{2}} \\
\dfrac{L}{\sqrt{2}} \\
0
\end{bmatrix}
\end{equation}

\begin{equation}
\mathbf{r}_2 =
\begin{bmatrix}
-\dfrac{L}{\sqrt{2}} \\
\dfrac{L}{\sqrt{2}} \\
0
\end{bmatrix}
\end{equation}

\begin{equation}
\mathbf{r}_3 =
\begin{bmatrix}
\dfrac{L}{\sqrt{2}} \\
-\dfrac{L}{\sqrt{2}} \\
0
\end{bmatrix}
\end{equation}

\begin{equation}
\mathbf{r}_4 =
\begin{bmatrix}
-\dfrac{L}{\sqrt{2}} \\
-\dfrac{L}{\sqrt{2}} \\
0
\end{bmatrix}
\end{equation}


\subsection{Torques producidos por cada rotor}

\subsubsection{Rotor 1}

\begin{align}
\mathbf{T}_1
&= \mathbf{r}_1 \times \mathbf{F}_{T_1} \\[2mm]
&=
\begin{vmatrix}
\hat{\mathbf{i}} & \hat{\mathbf{j}} & \hat{\mathbf{k}} \\
\dfrac{L}{\sqrt{2}} & \dfrac{L}{\sqrt{2}} & 0 \\
0 & 0 & -F_{T_1}
\end{vmatrix} \\[3mm]
&=
\begin{bmatrix}
-\dfrac{L}{\sqrt{2}}F_{T_1} \\
\dfrac{L}{\sqrt{2}}F_{T_1} \\
0
\end{bmatrix}
\end{align}


\subsubsection{Rotor 2}

\begin{align}
\mathbf{T}_2
&= \mathbf{r}_2 \times \mathbf{F}_{T_2} \\[2mm]
&=
\begin{vmatrix}
\hat{\mathbf{i}} & \hat{\mathbf{j}} & \hat{\mathbf{k}} \\
-\dfrac{L}{\sqrt{2}} & \dfrac{L}{\sqrt{2}} & 0 \\
0 & 0 & -F_{T_2}
\end{vmatrix} \\[3mm]
&=
\begin{bmatrix}
-\dfrac{L}{\sqrt{2}}F_{T_2} \\
-\dfrac{L}{\sqrt{2}}F_{T_2} \\
0
\end{bmatrix}
\end{align}


\subsubsection{Rotor 3}

\begin{align}
\mathbf{T}_3
&= \mathbf{r}_3 \times \mathbf{F}_{T_3} \\[2mm]
&=
\begin{vmatrix}
\hat{\mathbf{i}} & \hat{\mathbf{j}} & \hat{\mathbf{k}} \\
\dfrac{L}{\sqrt{2}} & -\dfrac{L}{\sqrt{2}} & 0 \\
0 & 0 & -F_{T_3}
\end{vmatrix} \\[3mm]
&=
\begin{bmatrix}
\dfrac{L}{\sqrt{2}}F_{T_3} \\
\dfrac{L}{\sqrt{2}}F_{T_3} \\
0
\end{bmatrix}
\end{align}


\subsubsection{Rotor 4}

\begin{align}
\mathbf{T}_4
&= \mathbf{r}_4 \times \mathbf{F}_{T_4} \\[2mm]
&=
\begin{vmatrix}
\hat{\mathbf{i}} & \hat{\mathbf{j}} & \hat{\mathbf{k}} \\
-\dfrac{L}{\sqrt{2}} & -\dfrac{L}{\sqrt{2}} & 0 \\
0 & 0 & -F_{T_4}
\end{vmatrix} \\[3mm]
&=
\begin{bmatrix}
\dfrac{L}{\sqrt{2}}F_{T_4} \\
-\dfrac{L}{\sqrt{2}}F_{T_4} \\
0
\end{bmatrix}
\end{align}


La fuerza producida por cada rotor se define como:

\begin{equation}
\mathbf{F}_{T_i} =
\begin{bmatrix}
0 \\
0 \\
-F_{T_i}
\end{bmatrix}
\end{equation}


\subsection{Torques totales}

El torque total alrededor del eje $y$ está dado por:

\begin{align}
\tau_y
&= L F_{T_1} - L F_{T_2} + L F_{T_3} - L F_{T_4} \\
&= L\left(
F_{T_1} + F_{T_3}
- F_{T_2} - F_{T_4}
\right) \\
&= L K_T\left(
\Omega_1^2 + \Omega_3^2
- \Omega_2^2 - \Omega_4^2
\right)
\end{align}


El torque total alrededor del eje $x$ está dado por:

\begin{align}
\tau_x
&= L\left(
F_{T_2} + F_{T_3}
- F_{T_1} - F_{T_4}
\right) \\
&= L K_T\left(
\Omega_2^2 + \Omega_3^2
- \Omega_1^2 - \Omega_4^2
\right)
\end{align}


El torque total alrededor del eje $z$ está dado por:

\begin{align}
\tau_z
&= Q_1 + Q_2 - Q_3 - Q_4 \\
&= K_Q\left(
\Omega_1^2 + \Omega_2^2
- \Omega_3^2 - \Omega_4^2
\right)
\end{align}


La relación entre el torque de reacción $Q$ y la velocidad angular del rotor es:

\begin{equation}
Q = K_Q \Omega^2
\end{equation}


Finalmente, la fuerza total producida por los cuatro rotores es:

\begin{equation}
F_T =
K_T\left(
\Omega_1^2 + \Omega_2^2
+ \Omega_3^2 + \Omega_4^2
\right)
\end{equation}




\end{document}
